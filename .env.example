# PostgreSQL
POSTGRES_USER=finanz
POSTGRES_PASSWORD=finanz
POSTGRES_DB=finanz
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# URL completa (opcional). Si se define, tiene prioridad sobre las variables de arriba.
# DATABASE_URL=postgresql+psycopg2://finanz:finanz@localhost:5432/finanz

# MongoDB (auditoría - opcional)
MONGO_URL=mongodb://localhost:27017
MONGO_DB=finanz_audit
